import { AppResaltadoDirective } from './app-resaltado.directive';

describe('AppResaltadoDirective', () => {
  it('should create an instance', () => {
    const directive = new AppResaltadoDirective();
    expect(directive).toBeTruthy();
  });
});
